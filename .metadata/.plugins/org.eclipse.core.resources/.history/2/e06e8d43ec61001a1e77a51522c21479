package operation;

import domain.Position;

public class PositionUtil implements Cloneable {
	public static Position sub(Position positiona, Position positionb) {
		Position position = (Position) positiona.clone();
		return position.sub(pointb);
	}
}
