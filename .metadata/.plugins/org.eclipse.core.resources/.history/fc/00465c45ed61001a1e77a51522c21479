package operation;

import domain.Position;

public class PositionUtil implements Cloneable {
	public static Position sub(double positiona, double positionb) {
		Position position = new Position(dimension, range);
		return position.sub(pointb);
	}
}
